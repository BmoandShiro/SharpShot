<Window x:Class="SharpShot.UI.RegionSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Region Selection"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="Black"
        ShowActivated="False"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    
    <Canvas x:Name="SelectionCanvas" Background="Black">
        <!-- Frozen desktop snapshot so open menus/dropdowns stay capturable even if focus changes -->
        <Image x:Name="FreezeFrameImage"
               Stretch="Fill"
               IsHitTestVisible="False"/>
        <Rectangle x:Name="FreezeDimOverlay"
                   Fill="#40000000"
                   IsHitTestVisible="False"/>
        <Rectangle x:Name="DimBandTop" Fill="#40000000" IsHitTestVisible="False" Visibility="Collapsed"/>
        <Rectangle x:Name="DimBandLeft" Fill="#40000000" IsHitTestVisible="False" Visibility="Collapsed"/>
        <Rectangle x:Name="DimBandRight" Fill="#40000000" IsHitTestVisible="False" Visibility="Collapsed"/>
        <Rectangle x:Name="DimBandBottom" Fill="#40000000" IsHitTestVisible="False" Visibility="Collapsed"/>
        <!-- Smart region highlights (images, text blocks) - drawn when option is enabled -->
        <Canvas x:Name="SmartHighlightsCanvas" IsHitTestVisible="False"/>
        <Rectangle x:Name="SmartHoverRect"
                   Stroke="{DynamicResource AccentBrush}"
                   StrokeThickness="2"
                   Fill="Transparent"
                   Visibility="Collapsed"
                   IsHitTestVisible="False"/>
        <Rectangle x:Name="SelectionRect" 
                   Stroke="{StaticResource AccentBrush}" 
                   StrokeThickness="2" 
                   StrokeDashArray="5,5"
                   Fill="Transparent"
                   Visibility="Collapsed"/>
        <Canvas x:Name="AdjustGripsCanvas" IsHitTestVisible="False" Visibility="Collapsed"/>
        
        <!-- Instructions -->
        <TextBlock x:Name="InstructionsText"
                   Text="Click and drag to select a region, or press ESC to cancel"
                   Foreground="{StaticResource TextBrush}"
                   FontSize="16"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Canvas.Top="50"/>
    </Canvas>
</Window> 